    <Window x:Class="Ion.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ion"
        mc:Ignorable="d"
        WindowStyle="None" AllowsTransparency="True"
        SizeToContent="Manual" ResizeMode="CanResize" 
        Width="800" Height="450"
        MinWidth="650" MinHeight="400"
        Background="Transparent">

    <WindowChrome.WindowChrome>
        <WindowChrome x:Name="WindowChrome"
            CaptionHeight="0"
            ResizeBorderThickness="6"
            GlassFrameThickness="0"
            UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>

                <ResourceDictionary Source="/Resources/Settings/ErrorLogsStyle.xaml"/>
                <ResourceDictionary Source="/Resources/Settings/TextBoxStyle.xaml"/>
                <ResourceDictionary Source="/Resources/Settings/TextBlockStyle.xaml"/>
                <ResourceDictionary Source="/Resources/Settings/CheckBoxStyle.xaml"/>
                <ResourceDictionary Source="/Resources/Settings/RadioButtonStyle.xaml"/>
                <ResourceDictionary Source="/Resources/Settings/ButtonStyle.xaml"/>

                <ResourceDictionary Source="/Resources/WindowsControl/WindowControlButtonStyle.xaml"/>
                <ResourceDictionary Source="/Resources/TabList/Tab.xaml"/>
                
                <ResourceDictionary Source="/Resources/Menu/SeparatorStyle.xaml"/>
                <ResourceDictionary Source="/Resources/Menu/MenuItemStyle.xaml"/>
                <ResourceDictionary Source="/Resources/Menu/SubMenuItemStyle.xaml"/>
                
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!--<Window.Background>
    <ImageBrush x:Name="BackgroundImage" 
            ImageSource="E:\Basic\Image\Night-Sky-Colorful-Beautiful-Clouds-Mountain-4k-Wallpaper-scaled.jpg"
            Stretch="Uniform"/>
    </Window.Background>-->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" MinWidth="100"/>
                </Grid.ColumnDefinitions>


                <Button x:Name="MoveTabLeft" Grid.Column="0" Visibility="Collapsed" Content="&lt;"/>

                <Border Grid.Column="1" ClipToBounds="True">
                    <TabControl x:Name="TabList"
                        ItemsSource="{Binding Tabs}"
                        SelectedItem="{Binding SelectedTab, Mode=TwoWay}">

                        <TabControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </TabControl.ItemsPanel>

                        <TabControl.Template>
                            <ControlTemplate TargetType="TabControl">
                                <Grid>

                                    <ScrollViewer Background="#111111"
                                        HorizontalScrollBarVisibility="Auto"
                                        VerticalScrollBarVisibility="Disabled">
                                        <StackPanel Orientation="Horizontal">
                                            <ItemsPresenter/>
                                            <Button Style="{StaticResource AddTabButtonStyle}" Click="AddTab"/>
                                        </StackPanel>
                                    </ScrollViewer>

                                </Grid>
                            </ControlTemplate>
                        </TabControl.Template>

                        <TabControl.ItemContainerStyle>
                            <Style TargetType="TabItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TabItem">
                                            <Border Style="{StaticResource TabItemStyle}">
                                                <Grid Style="{StaticResource TabContentStyle}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Column="0" 
                                                        Text="{Binding _Header, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                                        Style="{StaticResource TabHeaderStyle}"/>

                                                    <TextBlock Grid.Column="1"
                                                        Text="*"
                                                        Visibility="{Binding _SavedIndicator, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                                        Style="{StaticResource TabSavedIndicatorStyle}"/>

                                                    <Button Grid.Column="2"
                                                        Click="CloseTab"
                                                        Tag="{Binding}" Style="{StaticResource CloseTabButtonStyle}"/>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TabControl.ItemContainerStyle>
                    </TabControl>
                </Border>

                <Button x:Name="MoveTabRight" Grid.Column="2" Visibility="Collapsed" Content=">"/>
                

                <Border x:Name="DragArea" Grid.Column="3" MinWidth="150" Margin="-1 0 0 0"
                    Background="#111113" MouseLeftButtonDown="DragWindow"/>
            </Grid>

            <StackPanel Grid.Column="1" Orientation="Horizontal"
                HorizontalAlignment="Right" VerticalAlignment="Top">

                <Border Grid.Column="1" MinWidth="150"
                    Background="#111113" MouseLeftButtonDown="DragWindow"/>

                <Button x:Name="MinimizeButton" 
                    Style="{StaticResource WindowControlButton}"
                    Content="─"
                    Click="MinimizeWindow" FontSize="16"/>

                <Button x:Name="MaximizeButton" 
                    Style="{StaticResource WindowControlButton}"
                    Content="❐" 
                    Click="MaximizeWindow" FontSize="13"/>

                <Button x:Name="CloseButton" 
                    Style="{StaticResource WindowControlButton}"
                    Content="✕"
                    Click="Exit"/>
            </StackPanel>
        </Grid>

        <Menu x:Name="Menu" Grid.Row="1" Background="#2F2F36" Foreground="White" FontSize="14" Margin="0 -1 0 0">
            <Menu.Resources>
                <Style TargetType="MenuItem" BasedOn="{StaticResource MenuItemStyle}"/>

                <Style TargetType="ContextMenu">
                    <Setter Property="Background" Value="#1E1E1E"/>
                    <Setter Property="BorderBrush" Value="#454545"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </Menu.Resources>

            <MenuItem x:Name="FileMenu" Header="File">
                <MenuItem Header="New..." InputGestureText="Ctrl+N" Style="{StaticResource ResourceKey=SubMenuItemStyle}" Tag="New">

                    <MenuItem Header="Text formats" Style="{StaticResource ResourceKey=SubMenuItemStyle}">
                        <MenuItem Header=".txt" InputGestureText="Text file"/>
                        <MenuItem Header=".log" InputGestureText="Log file"/>
                        <MenuItem Header=".ini" InputGestureText="Configuration file"/>
                        <MenuItem Header=".cfg" InputGestureText="Configuration file"/>
                        <MenuItem Header=".conf" InputGestureText="Configuration file"/>
                    </MenuItem>

                    <MenuItem Header="Programming languages" Style="{StaticResource ResourceKey=SubMenuItemStyle}">
                        <MenuItem Header=".cs" InputGestureText="C# source code"/>
                        <MenuItem Header=".cpp" InputGestureText="C++ source code"/>
                        <MenuItem Header=".java" InputGestureText="Java source code"/>
                        <MenuItem Header=".js" InputGestureText="JavaScript file"/>
                        <MenuItem Header=".py" InputGestureText="Python script"/>
                        <MenuItem Header=".kt" InputGestureText="Kotlin source file"/>
                        <MenuItem Header=".swift" InputGestureText="Swift source file"/>
                        <MenuItem Header=".go" InputGestureText="Go source file"/>
                        <MenuItem Header=".rs" InputGestureText="Rust source file"/>
                        <MenuItem Header=".lua" InputGestureText="Lua script"/>
                        <MenuItem Header=".rb" InputGestureText="Ruby script"/>
                            <Separator Style="{StaticResource SeparatorStyle}"/>
                        <MenuItem Header=".html" InputGestureText="HyperText Markup Language"/>
                        <MenuItem Header=".css" InputGestureText="Cascading Style Sheets"/>
                        <MenuItem Header=".sh" InputGestureText="Bash shell script"/>
                        <MenuItem Header=".ps1" InputGestureText="PowerShell script"/>
                        <MenuItem Header=".sql" InputGestureText="Structured Query Language"/>
                    </MenuItem>

                    <MenuItem Header="Structure data" Style="{StaticResource ResourceKey=SubMenuItemStyle}">
                        <MenuItem Header=".json" InputGestureText="JavaScript Object Notation"/>
                        <MenuItem Header=".xaml" InputGestureText="eXtensible Application Markup Language"/>
                        <MenuItem Header=".xml"  InputGestureText="eXtensible Markup Language"/>
                        <MenuItem Header=".csv"  InputGestureText="Comma-separated values"/>
                        <MenuItem Header=".yaml" InputGestureText="YAML Ain't Markup Language"/>
                        <MenuItem Header=".yml"  InputGestureText="YAML configuration file"/>
                    </MenuItem>

                </MenuItem>
                <MenuItem Header="Open"       InputGestureText="Ctrl+O"       Tag="Open"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Save"       InputGestureText="Ctrl+S"       Tag="Save"/>
                <MenuItem Header="Save as..." InputGestureText="Ctrl+Shift+S" Tag="SaveAs"/>
                <MenuItem Header="Save all"   InputGestureText="Alt+S"        Tag="SaveAll"/>
                <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Reload"     InputGestureText="Ctrl+R"       Tag="Reload" Name="ReloadButton"/>
                <MenuItem Header="Print..."   InputGestureText="Ctrl+P"       Tag="Print"/>
                <MenuItem Header="Send"       InputGestureText="Ctrl+T"       Tag="Send" IsEnabled="False"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Exit"       InputGestureText="Alt+F4"       Tag="Exit"/>
            </MenuItem>

            <MenuItem x:Name="EditMenu" Header="Edit">
                <MenuItem Header="To lower"       InputGestureText="Ctrl+Shift+L" Tag="ToLower"/>
                <MenuItem Header="To upper"       InputGestureText="Ctrl+Shift+U" Tag="ToUpper"/>
                <MenuItem Header="Capitalize"     InputGestureText="Ctrl+Shift+C" Tag="Capitalize"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Do"             InputGestureText="Ctrl+D"       Tag="Do" IsEnabled="False"/>
                <MenuItem Header="Duplicate line" InputGestureText="Ctrl+Shift+D" Tag="DuplicateLine"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Highlight"      InputGestureText="Ctrl+M"       Tag="Highlight"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Level up"       InputGestureText="Tab"          Tag="LevelUp"/>
                <MenuItem Header="Level down"     InputGestureText="Shift+Tab"    Tag="LevelDown"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Up digit"       InputGestureText="Ctrl+Up"      Tag="UpDigit"/>
                <MenuItem Header="Down digit"     InputGestureText="Ctrl+Down"    Tag="DownDigit"/>
                <MenuItem Header="Normal digit"   InputGestureText="Ctrl+0"       Tag="NormalizeDigit"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="ToUnicode"      InputGestureText="Ctrl+U"      Tag="ToUnicode"/>
                <MenuItem Header="Convert chars"  InputGestureText="Ctrl+T"      Tag="ConvertChars"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Information"    InputGestureText="Ctrl+I"       Tag="GetInformation"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Find..."        InputGestureText="Ctrl+F"       Tag="Find"/>
                <MenuItem Header="Replace..."     InputGestureText="Ctrl+H"       Tag="Replace"/>
                <!--<MenuItem Header="Go to..." InputGestureText="Ctrl+G" Tag="GoTo"/>-->
            </MenuItem>

            <MenuItem x:Name="InsertsMenu" Header="Inserts">
                <MenuItem Header="Time/date"     InputGestureText="F5" Tag="InsertTimeDate"/>
                <MenuItem Header="Only date"     Tag="InsertDate"/>
                <MenuItem Header="Only time"     Tag="InsertTime"/>
                <MenuItem Header="Calendar"      Tag="InsertCalendar"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Email"         InputGestureText="Ctrl+E" Tag="InsertEmail"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="SpecialChars"  Tag="SpecialChars"/>
            </MenuItem>

            <MenuItem x:Name="PunctuationMenu" Header="Punctuation">
                <MenuItem Header="Trim"                InputGestureText="Ctrl+Shift+T"      Tag="Trim"/>
                <MenuItem Header="Remove empty lines"  InputGestureText="Ctrl+Shift+Delete" Tag="RemoveEmptyLines"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Check brackets"      InputGestureText="Ctrl+Shift+B"      Tag="CheckBrackets"/>
                <MenuItem Header="Correct spaces"      InputGestureText="Ctrl+Shift+K"      Tag="CorrectSpaces"/>
                <MenuItem Header="Correct punctuation" InputGestureText="Ctrl+Shift+P"      Tag="CorrectPunctuation"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Join lines"          InputGestureText="Ctrl+J"            Tag="JoinLines"/>
            </MenuItem>

            <MenuItem x:Name="StructuringMenu" Header="Structuring">
                <MenuItem Header="Enumerate"       InputGestureText="Ctrl+ ;"       Tag="Enumerate"/>
                <MenuItem Header="De-enumerate"    InputGestureText="Ctrl+Shift+ ;" Tag="Enumerate"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="To structure"    InputGestureText="Ctrl+W"        Tag="ToStructure"/>
                <MenuItem Header="From structure"  InputGestureText="Ctrl+Shift+W"  Tag="FromStructure"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Group"           InputGestureText="Ctrl+G"        Tag="Group"/>
                <MenuItem Header="Ungroup"         InputGestureText="Ctrl+Shift+G"  Tag="Ungroup"/>
            </MenuItem>

            <MenuItem x:Name="ClipboardMenu" Header="Clipboard">
                <MenuItem Header="Undo"       InputGestureText="Ctrl+Z" Command="Undo"/>
                <MenuItem Header="Redo"       InputGestureText="Ctrl+Y" Command="Redo"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Cut"        InputGestureText="Ctrl+X" Command="Cut"/>
                <MenuItem Header="Copy"       InputGestureText="Ctrl+C" Command="Copy"/>
                <MenuItem Header="Paste"      InputGestureText="Ctrl+V" Command="Paste"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Select All" InputGestureText="Ctrl+A" Command="SelectAll"/>
            </MenuItem>

            <MenuItem x:Name="TabsMenu" Header="Tabs">
                <MenuItem Header="Next tab"      InputGestureText="Alt+Right"  Tag="NextTab"/>
                <MenuItem Header="Previous tab"  InputGestureText="Alt+Left"   Tag="PreviousTab"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Open last tab" InputGestureText="Alt+0"      Tag="OpenLastTab"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Close current" InputGestureText="Alt+C"      Tag="CloseCurrentTab"/>
                <MenuItem Header="Close saved"   InputGestureText="Ctrl+Alt+C" Tag="CloseSavedTabs"/>
            </MenuItem>

            <MenuItem x:Name="ViewMenu" Header="View">
                <MenuItem Header="Zoom in" x:Name="ZoomInButton"   InputGestureText="Ctrl+Plus"  Tag="ZoomInX5"/>
                <MenuItem Header="Zoom out" x:Name="ZoomOutButton" InputGestureText="Ctrl+Minus" Tag="ZoomOutX5"/>
                <MenuItem Header="Default zoom" InputGestureText="Ctrl+0" Tag="SetDefaultZoom"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Close side bar"                  InputGestureText="Ctrl+B"     Tag="CloseSideBar"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Centering"       Click="CenteringWindow"/>
                <MenuItem Header="Margin maximize" Click="MarginMaximizeWindow"/>
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                <MenuItem Header="Logs"            Tag="OpenLoggingMenu"/>
                <MenuItem Header="Settings"        Tag="OpenSettingsMenu"/>
            </MenuItem>

            <MenuItem x:Name="HelpMenu" Header="Help">
                <MenuItem Header="About editor"          Tag="AboutEditor"/>
                <MenuItem Header="About chars converter" Tag="AboutCharsConverter"/>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="2" Background="#D51A1A1F">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <RichTextBox x:Name="TextEditor" Grid.Column="0"
                Background="Transparent" Foreground="White" CaretBrush="White"
                FontSize="20" FontFamily="Consolas"
                Block.LineHeight="1" AcceptsReturn="True" AcceptsTab="True"
                HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                BorderThickness="0" Padding="6 10 6 10">
                <RichTextBox.Resources>
                    <Style TargetType="RichTextBox">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="RichTextBox">
                                    <Border 
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Background="{TemplateBinding Background}">
                                        <ScrollViewer x:Name="PART_ContentHost"
                                            HorizontalScrollBarVisibility="Auto"
                                            VerticalScrollBarVisibility="Auto"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </RichTextBox.Resources>
            </RichTextBox>

            <Border x:Name="SideBar" Grid.Column="1"
                    HorizontalAlignment="Right" Width="350"
                    Background="#A51A1A1A" Visibility="Collapsed"
                    CornerRadius="12 0 0 12" BorderThickness="2 2 0 2" BorderBrush="#2F2F2F">

                <Border.RenderTransform>
                    <TranslateTransform X="350"/>
                </Border.RenderTransform>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="SideBarHeader" Grid.Row="0" Margin="8 4 4 4"
                        Foreground="White" FontSize="18" FontWeight="Bold"/>

                    <Separator Grid.Row="1" Style="{StaticResource SeparatorStyle}" Background="#2F2F2F"/>

                    <Grid Grid.Row="2" Margin="6 -2 0 0">
                        <StackPanel x:Name="SettingsMenu" Visibility="Collapsed">
                            <TextBlock Text="Стандартное расширение файла:" Style="{StaticResource TextBlockStyle}"/>
                                <TextBox x:Name="Settings_BaseFileExtension" Style="{StaticResource TextBoxStyle}"/>


                            <TextBlock Text="Email:" Style="{StaticResource TextBlockStyle}" Margin="5 20 0 0"/>
                                <TextBox x:Name="Settings_Email" Style="{StaticResource TextBoxStyle}"/>
                            <TextBlock Text="Пароль приложения почты:" Style="{StaticResource TextBlockStyle}"/>
                                <TextBox x:Name="Settings_EmailPassword" Style="{StaticResource TextBoxStyle}"/>

                            
                            <CheckBox x:Name="Settings_SpellCheck" Content="Проверка орфографии" Style="{StaticResource CheckBoxStyle}" Margin="5 20 0 0"/>
                        </StackPanel>

                        
                        <StackPanel x:Name="SearchingMenu" Margin="10" Visibility="Collapsed">
                            <TextBox  x:Name="SearchingMenu_Target"                  Style="{StaticResource TextBoxStyle}" Margin="0 0 0 15"/>
                            <CheckBox x:Name="SearchingMenu_CaseSensitive"           Content="Учитывать регистр" Style="{StaticResource CheckBoxStyle}"/>
                            <CheckBox x:Name="SearchingMenu_WholeWord"               Content="Слово целиком" Style="{StaticResource CheckBoxStyle}"/>
                            <CheckBox x:Name="SearchingMenu_UsingUnicodeChars"       Content="Использовать экранированные символы" Style="{StaticResource CheckBoxStyle}"/>
                            <CheckBox x:Name="SearchingMenu_UsingRegularExpressions" Content="Использовать регулярные выражения" Style="{StaticResource CheckBoxStyle}"/>
                            <!--<CheckBox x:Name="SearchingMenu_OnlySelection"           Content="Только выделенный текст" Style="{StaticResource CheckBoxStyle}"/>-->
                        </StackPanel>


                        <StackPanel x:Name="ReplacingMenu" Margin="10" Visibility="Collapsed">
                            <TextBox x:Name="ReplacingMenu_From" Style="{StaticResource TextBoxStyle}" Margin="0 0 0 4"/>
                            <TextBlock Text="Заменить на:"       Style="{StaticResource TextBlockStyle}" Margin="10 0 0 2.5"/>
                            <TextBox x:Name="ReplacingMenu_To"   Style="{StaticResource TextBoxStyle}" Margin="0 4 0 15"/>

                            <CheckBox x:Name="ReplacingMenu_CaseSensitive"           Content="Учитывать регистр" Style="{StaticResource CheckBoxStyle}"/>
                            <CheckBox x:Name="ReplacingMenu_WholeWord"               Content="Слово целиком" Style="{StaticResource CheckBoxStyle}"/>
                            <CheckBox x:Name="ReplacingMenu_UsingUnicodeChars"       Content="Использовать экранированные символы" Style="{StaticResource CheckBoxStyle}"/>
                            <CheckBox x:Name="ReplacingMenu_UsingRegulatExpressions" Content="Использовать регулярные выражения" Style="{StaticResource CheckBoxStyle}"/>
                        </StackPanel>


                        <StackPanel x:Name="SendingMenu" Margin="10" Visibility="Collapsed">
                            <TextBlock Text="Заголовок:" Style="{StaticResource TextBlockStyle}"/>
                            <TextBox x:Name="MessageHeader" Style="{StaticResource TextBoxStyle}" Margin="0 5 0 5"/>
                            
                            <TextBlock Text="Отправить:" Style="{StaticResource TextBlockStyle}"/>
                            <RadioButton x:Name="SendAllText"
                                Style="{StaticResource RadioButtonStyle}"
                                GroupName="SendGroup"
                                Content="Весь текст"
                                IsChecked="True"/>

                            <RadioButton x:Name="SendSelection"
                                Style="{StaticResource RadioButtonStyle}"
                                GroupName="SendGroup"
                                Content="Выделенный текст"/>
                        </StackPanel>


                        <Grid x:Name="LoggingMenu" Margin="10" Visibility="Collapsed">
                            <ListView x:Name="ErrorListView" Focusable="False" VerticalAlignment="Top"
                                Style="{StaticResource ErrorListViewStyle}"
                                ItemContainerStyle="{StaticResource ErrorListItemStyle}"
                                ItemsSource="{Binding ErrorLogs}"
                                MaxHeight="300">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Message}" 
                                            TextWrapping="Wrap"
                                            Padding="10 5"/>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>


                        <StackPanel x:Name="GoToMenu" Grid.Row="1" Margin="10" Visibility="Collapsed">
                            <TextBlock Text="Номер строки:" Style="{StaticResource TextBlockStyle}"/>
                            <TextBox x:Name="GoTo_LineIndex" Style="{StaticResource TextBoxStyle}"/>
                        </StackPanel>

                    </Grid>

                    <Grid Grid.Row="3" Height="34">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button x:Name="CancelButton" Grid.Column="0" Style="{StaticResource CancelButtonStyle}"/>
                        <Button x:Name="ApplyButton" Grid.Column="1" Style="{StaticResource ApplyButtonStyle}"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <TextBlock x:Name="MessageBox" Grid.Row="3"
            Height="Auto" Padding="2 2 0 2"
            Background="#4D4D56" Foreground="White"
            FontSize="14" FontFamily="Segoe UI" />
    </Grid>
</Window>