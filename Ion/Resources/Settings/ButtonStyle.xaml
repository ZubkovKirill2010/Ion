<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="SettingsButtonStyle" TargetType="Button">
        <Setter Property="Padding" Value="10 7"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border" 
                            Background="{TemplateBinding Background}"
                            CornerRadius="4">
                        
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Opacity" Value="0.8"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CancelButtonStyle" BasedOn="{StaticResource SettingsButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="#6E6E6E"/>
        <Setter Property="BorderBrush" Value="#636363"/>

        <Setter Property="Margin" Value="8 0 2 8"/>
        <Setter Property="Content" Value="Cancel"/>
    </Style>

    <Style x:Key="ApplyButtonStyle" BasedOn="{StaticResource SettingsButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="#27b2ad"/>
        <Setter Property="BorderBrush" Value="#23938e"/>

        <Setter Property="Margin" Value="2 0 5 8"/>
        <Setter Property="Content" Value="Apply"/>
    </Style>

</ResourceDictionary>