<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Margin" Value="0 5"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid>
                        <Border x:Name="OuterCircle"
                            Width="20" 
                            Height="20"
                            CornerRadius="10"
                            BorderThickness="2"
                            BorderBrush="#7A7A7A"
                            Background="Transparent"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"/>

                        <Ellipse x:Name="InnerCircle"
                            Width="12"
                            Height="12"
                            Fill="Transparent"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            Margin="4 0 0 0"/>

                        <ContentPresenter x:Name="content"
                            Margin="30 0 0 0"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            RecognizesAccessKey="True"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#AAAAAA"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="InnerCircle" Property="Fill" Value="#26C9C6"/>
                            <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#26C9C6"/>

                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="InnerCircle"
                                            Storyboard.TargetProperty="Opacity"
                                            From="0" To="1" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="False">
                            <Setter TargetName="InnerCircle" Property="Fill" Value="Transparent"/>
                            <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#7A7A7A"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="OuterCircle" Property="Opacity" Value="0.5"/>
                            <Setter TargetName="content" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>